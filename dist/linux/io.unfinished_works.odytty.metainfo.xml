<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.unfinished_works.odytty</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-only</project_license>
  <name>OdyTTY</name>
  <summary>GPU-rendered terminal emulator with an Odyssey visual identity</summary>
  <developer id="io.unfinished_works">
    <name>Unfinished Works</name>
  </developer>
  <launchable type="desktop-id">io.unfinished_works.odytty.desktop</launchable>
  <provides>
    <binary>odytty</binary>
  </provides>
  <categories>
    <category>System</category>
    <category>TerminalEmulator</category>
  </categories>
  <keywords>
    <keyword>terminal</keyword>
    <keyword>tty</keyword>
    <keyword>pty</keyword>
    <keyword>shell</keyword>
  </keywords>
  <description>
    <p>
      OdyTTY is a from-scratch Rust terminal emulator for Linux. It owns the
      terminal byte path from PTY allocation through escape parsing, terminal
      state, render geometry, and GPU presentation.
    </p>
    <p>
      The project explores a distinctive Odyssey visual identity while keeping
      terminal correctness, readable text, predictable input, local privacy, and
      performance as hard requirements.
    </p>
  </description>
  <url type="homepage">https://odytty.unfinished-works.com</url>
  <url type="bugtracker">https://github.com/ghreprimand/odytty/issues</url>
  <content_rating type="oars-1.1"/>
  <screenshots>
    <screenshot type="default">
      <image>https://raw.githubusercontent.com/ghreprimand/odytty/master/assets/demo.png</image>
      <caption>OdyTTY running a shell with the Odyssey visual identity</caption>
    </screenshot>
  </screenshots>
  <releases>
    <release version="0.8.1" date="2026-07-07">
      <description>
        <p>Point release focused on scroll feel and a copy fix. Scrollback scrolling gains an optional animated glide, on by default: the viewport jumps to its target row instantly while a separate visual position eases toward it each frame, moving only in the scroll direction so it can never overshoot or bounce. High-resolution and touchpad devices drive a separate continuous pixel-precise lane that tracks pointer travel directly. The mouse-wheel scroll amount is now six rows per notch by default and remains configurable; it governs both local scrollback and alternate-scroll pagers such as less, man, and git log so the two advance in step, while full mouse-tracking programs such as vim, tmux, and htop still own the wheel themselves. The composited tab bar and vertical workspace rail are pinned against the sub-row scroll motion, so only terminal content glides and the chrome no longer drifts with it. Copying a mouse selection that extends beyond the visible screen now captures the full selected range across scrollback, for both normal and rectangular block selection.</p>
      </description>
    </release>
    <release version="0.8.0" date="2026-07-07">
      <description>
        <p>Feature release. Tabs now live inside named workspaces, each with its own tab strip and a vertical rail that appears once a second workspace exists; workspaces can be created, renamed, closed, cycled by keyboard, and bound to a remote host. Opt-in layout persistence (off by default) reopens the previous window shape — workspaces, tabs, and pane splits at their working directories — when launched with no arguments; snapshots record structure only, never terminal output, scrollback, environment, or the commands that were running, so a restored pane is always a fresh shell. Named layouts capture the whole session and can be saved, reopened with a replace-or-add prompt, and on Unix reattach still-alive SSH sessions. Seamless remote terminals carry the shell integration onto a remote over an inline bash bootstrap with nothing persisted remotely, reuse SSH connections across tabs via ControlMaster, hold dropped connections open with a reconnect prompt, optionally persist sessions with tmux, and paste images through to the remote host. A rebuilt connection manager adds an Add/Edit form with a Test Connection probe and a per-host identity file, right-click host actions, and ad-hoc connection to an unsaved host. Right-click menus are composed per surface. The settings panel is compacted to one line per row with a focused-row help footer and split name/value click zones. Window transparency (off by default) makes the background translucent while text, cursor, selection, and overlays stay opaque. The tab chrome is reworked into a flat, glow-accented treatment with a top bar or a drag-resizable, auto-hiding vertical rail. Shutdown and rapid-close paths are bounded so a wedged remote can no longer stall exit. Windows: transparency composites through DWM; SSH connection reuse is a Unix-client feature and restored remote panes open fresh.</p>
      </description>
    </release>
    <release version="0.7.5" date="2026-07-02">
      <description>
        <p>Hardening and correctness release: a full audit of the terminal core, session host, graphics protocols, and native shell lands as thirty-plus individually tested fixes. Highlights: memory-safety ceilings on scrollback, image transfers, and the glyph atlas; Kitty graphics chunked transmissions and quiet modes; sixel and placement clipping fixes; VT conformance (IND/NEL, DECSTBM clamping, OSC color formats and palette queries); overlay and IME keystrokes no longer leak into the shell; prompt-aware select+Delete rebuilt on an explicit input-region model with per-shell certainty tiers; click-to-place cursor now on by default with multi-row wrapped-line support; crash/freeze hardening with a panic hook, watchdog, and default rotated logging. Windows: process trees are cleaned up via Job Objects, a ConPTY resize/close race is fixed, logs move to LOCALAPPDATA, missing symbol glyphs render via a Segoe UI Symbol fallback, and a "New Window" action and Keyboard Shortcuts menu item round out the UI.</p>
      </description>
    </release>
    <release version="0.7.1" date="2026-07-01">
      <description>
        <p>Bugfix release. Fixes an idle-window render freeze: a long-lived, non-interacted OdyTTY window could stop repainting entirely — stale pixels, input with no echo, and no repaint even when dragged to another monitor — after a transient GPU/compositor surface hiccup. The skipped-frame recovery gave up permanently once its fast-retry budget was spent, so under an event-wait loop a window with no incoming events never repainted again. The fix adds a slow keep-alive retry so an idle window self-heals within about a second of the surface recovering, without busy-spinning.</p>
      </description>
    </release>
    <release version="0.7.0" date="2026-07-01">
      <description>
        <p>Cursor, selection, and multi-pane hardening release, and the first with a Windows install channel (Scoop). Fixes resize cursor drift on pulled scrollback, minimize/restore black-screen and 1x1 reflow, prompt-aware select+Delete surviving a side-by-side split, clickable paths for filenames containing spaces, multi-pane wheel scrolling the pane under the cursor and hover resolving to the correct pane, drag-selection after a split, and a divider-drag pseudoconsole resize flood. Adds PowerShell shell integration (OSC 133 marks) and OSC 7 working-directory reporting on Windows. Interactive Windows behaviour is validated on-device; Linux, macOS, and Windows are all blocking CI legs.</p>
      </description>
    </release>
    <release version="0.6.2" date="2026-06-28">
      <description>
        <p>First experimental Windows build: OdyTTY compiles and runs on Windows through a ConPTY pseudoconsole backend, shipping as an unsigned portable x86_64 zip alongside the existing source, AppImage, and AUR artifacts. Local tabs, splits, rendering, themes, effects, inline graphics, and persistent configuration under %APPDATA% work; detachable/resumable session hosting stays Unix-only for this release.</p>
      </description>
    </release>
    <release version="0.6.1" date="2026-06-28">
      <description>
        <p>Fixes settings-description drift so each setting's help text matches its actual behaviour, and restores the GUI "reset to default background" action.</p>
      </description>
    </release>
    <release version="0.6.0" date="2026-06-27">
      <description>
        <p>Ships a bundled default background and OdysseyOS identity defaults, so a fresh install opens with the intended Odyssey visual identity out of the box.</p>
      </description>
    </release>
    <release version="0.5.4" date="2026-06-27">
      <description>
        <p>Adds an in-app About section to the settings overlay: a read-only view (the last row of the settings list) showing version, build provenance (git commit, build date, target, Rust version), and live GPU/renderer diagnostics (adapter, backend, driver), plus clickable project links and a one-click "Copy diagnostics" for bug reports. The diagnostics block deliberately omits filesystem paths so it can be shared without leaking a username.</p>
      </description>
    </release>
    <release version="0.5.3" date="2026-06-26">
      <description>
        <p>Adds two opt-in ergonomic features and keyboard parity for prompt editing: a smart Ctrl+C mode (copy-or-interrupt) that copies a local selection when one exists and otherwise sends the interrupt, clickable bare URLs (Ctrl+click to open printed http/https links, on by default), and Delete/Backspace removing a selected editable prompt input via the same shell-integration-gated path as the right-click Delete. Ships a best-effort x86_64 AppImage and an AUR package alongside the source release.</p>
      </description>
    </release>
    <release version="0.5.2" date="2026-06-26">
      <description>
        <p>One-fix patch release: the native window now announces its application id (io.unfinished_works.odytty) on Linux, so Wayland compositors and X11 bars resolve the OdyTTY taskbar/dock icon instead of a generic placeholder. The desktop file gains a matching StartupWMClass so spawned windows group under their launcher entry.</p>
      </description>
    </release>
    <release version="0.5.1" date="2026-06-26">
      <description>
        <p>Visual-polish release: Nerd Font / symbol icon glyphs are now scaled aspect-preserving and centered in their cell so icon columns (e.g. a fastfetch panel) render even and uncropped, while body text stays byte-identical. OdyTTY also self-identifies to terminal-aware tooling by exporting TERM_PROGRAM=odytty and TERM_PROGRAM_VERSION.</p>
      </description>
    </release>
    <release version="0.5.0" date="2026-06-26">
      <description>
        <p>Bug-fix and UX release validated hands-on on both Linux and macOS: click-to-open now works on a real desktop (Ctrl+click on Linux, Cmd+click on macOS), closing the v0.4.0 cross-platform regressions, and extends the interactive-paths workflow layer.</p>
      </description>
    </release>
    <release version="0.4.0" date="2026-06-24">
      <description>
        <p>Brings multi-tab and split-pane multiplexing (each pane keeps its own scrollback, selection, viewport, and search), detached resumable sessions with reattach driven by a local-only, per-user Unix socket, and an in-window Manage Sessions overlay (default Ctrl+Shift+A) for attach, rename, kill, and Detach and switch. Adds an SSH connection manager, a Ctrl+Shift+P command palette, and a scrubbable session-replay overlay. Introduces interactive paths (default off): with the gate enabled, OdyTTY detects filesystem paths in its output and Ctrl+click opens them — files via xdg-open, path:line:col via an editor matrix, directories via the file manager — plus Open, Open With…, Copy Path, Copy File, and Reveal in File Manager context items. Resolved image paths open in an in-app lightbox viewer (Esc or click-outside to dismiss). The in-panel keybinding editor now covers every bindable action.</p>
      </description>
    </release>
    <release version="0.3.1" date="2026-06-24">
      <description>
        <p>Makes four overlays discoverable by giving each a default Ctrl+Shift chord and a menu entry: Ctrl+Shift+P opens the command palette, Ctrl+Shift+S the connection manager, Ctrl+Shift+R the session-replay overlay, and Ctrl+Shift+B the theme builder. All four use Ctrl+Shift+&lt;letter&gt; chords that a TUI cannot receive, so no PTY input path is perturbed, and every chord remains rebindable. Prompt navigation stays on the Ctrl+Shift+Up / Ctrl+Shift+Down arrow chords.</p>
      </description>
    </release>
    <release version="0.3.0" date="2026-06-23">
      <description>
        <p>Lands native splits and panes on a per-tab binary layout tree (single-pane tabs stay byte-identical), reachable via tmux-style prefix bindings and direct Ctrl+Shift+E / Ctrl+Shift+O chords with drag-resizable dividers and directional focus moves. Adds resumable, persistent sessions: a detached session-host process owns the PTYs and terminal models so a window can close and reattach with full scrollback over a per-user, local-only Unix socket, with CLI verbs odytty new, list, and attach. Also introduces an SSH connection manager and a command palette.</p>
      </description>
    </release>
    <release version="0.2.1" date="2026-06-21">
      <description>
        <p>Fixes insert/replace mode (IRM, ANSI mode 4): printing in insert mode now shifts existing cells to the right instead of overwriting them, so editors that rely on incremental line redraw — notably Apple's pico/nano on macOS — no longer corrupt the current line. Also bounds scrollback memory by default (a runaway program can no longer grow history until the system runs out of memory) and makes the test suite deterministic. No change to terminal behavior beyond the IRM fix.</p>
      </description>
    </release>
    <release version="0.2.0" date="2026-06-20">
      <description>
        <p>Adds a mouse-cursor shape (an I-beam over the text grid, a hand over hyperlinks, an arrow over chrome), fixes a stuck-underline bug from a misparsed private-mode escape sequence, and improves macOS symbol-glyph and color coverage. OdyTTY now ships as source only on both Linux and macOS: the prebuilt macOS disk image is retired in favor of a build-from-source flow, and both platforms are exercised in continuous integration.</p>
      </description>
    </release>
    <release version="0.1.9" date="2026-06-19">
      <description>
        <p>Fixes scrollback not filling when a program leaves a full-screen scroll region set (so the mouse wheel and Shift+PageUp could not scroll back), adds alternate scroll mode so the wheel scrolls full-screen TUIs like pagers, discovers Apple Color Emoji on macOS, persists first-run onboarding dismissal, and builds from source on older stable Rust toolchains.</p>
      </description>
    </release>
    <release version="0.1.8" date="2026-06-19">
      <description>
        <p>Adds an experimental universal macOS build and a tag-triggered release pipeline that publishes the Linux source archive and the macOS disk image together. No functional change for Linux.</p>
      </description>
    </release>
    <release version="0.1.7" date="2026-06-19">
      <description>
        <p>Adds bell handling (visual flash or window urgency, configurable) and IME composition input so CJK input methods and compose-key accents work.</p>
      </description>
    </release>
    <release version="0.1.6" date="2026-06-19">
      <description>
        <p>Makes the bundled Nerd Font glyph pack render across both v2 and v3 codepoint eras via an ordered symbol fallback chain, and groups the font picker into bundled and system fonts.</p>
      </description>
    </release>
    <release version="0.1.5" date="2026-06-18">
      <description>
        <p>Adds geometric Unicode Braille pattern rendering so btop and other Braille-graph terminal output does not fall back to missing-glyph boxes.</p>
      </description>
    </release>
    <release version="0.1.4" date="2026-06-18">
      <description>
        <p>Fixes uneven background-image opacity in the padding and non-grid edge regions when translucent cell backgrounds are enabled.</p>
      </description>
    </release>
    <release version="0.1.3" date="2026-06-18">
      <description>
        <p>Adds `odytty -e command args...`, `--working-directory`, and `--title` launch support for terminal-exec integrations and command launchers.</p>
      </description>
    </release>
    <release version="0.1.2" date="2026-06-18">
      <description>
        <p>Fixes a native-session shutdown crash that could panic after the final shell exited while the event loop was still processing teardown.</p>
      </description>
    </release>
    <release version="0.1.1" date="2026-06-18">
      <description>
        <p>Refreshes the public README install guide so the release archive matches the documented user-local install flow.</p>
      </description>
    </release>
    <release version="0.1.0" date="2026-06-18">
      <description>
        <p>Initial Linux pre-release target with source packaging, desktop launcher metadata, and hicolor icon assets.</p>
      </description>
    </release>
  </releases>
</component>
