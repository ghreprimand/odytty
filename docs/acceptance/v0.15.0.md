# v0.15.0 hands-on acceptance

Hands-on pass/fail checklists for the four v0.15.0 feature surfaces. Record each
step as **PASS**, **FAIL**, **SKIP** (with reason), or **UNSUPPORTED**. Do not
claim a platform contract from unit tests alone.

Companion: [manual validation](../manual-validation.md),
[v0.15.0 foundation](../v0.15.0-foundation.md),
[release notes draft](../releases/0.15.0.md).

Quoted strings below are copied from the tree at the checklist authoring
revision. If a notice differs in a later build, treat the running binary as
authoritative and treat the difference as a documentation defect.

---

## 1. Quick terminal

**Prerequisites:** set `quick_terminal = on` and configure
`quick_terminal_shortcut` (default `F12`). Other knobs under test:
`quick_terminal_edge`, `quick_terminal_coverage`, `quick_terminal_span`,
`quick_terminal_monitor`, `quick_terminal_animation`,
`quick_terminal_hide_on_focus_loss`, `quick_terminal_profile`. Restart after
changing non-reloadable knobs. Palette row when enabled:
`Toggle Quick Terminal`.

### 1.1 Linux X11

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| QT-X11-01 | Linux X11 | Start with `quick_terminal = on`. After the first usable frame, press the configured global shortcut. | Quick terminal appears from the configured edge; a usable prompt is ready. | No window, delayed first ordinary window, or silent no-op. |
| QT-X11-02 | Linux X11 | Press the same shortcut again. | Window hides; the quick session stays alive (no shell restart). | Session restarts, PTY exits, or a second quick window appears. |
| QT-X11-03 | Linux X11 | Summon, type a unique marker, hide, summon again. | Marker text and cursor state remain. | Fresh shell or lost scrollback. |
| QT-X11-04 | Linux X11 | Rapidly press the shortcut 10+ times, including while the first window is appearing. | At most one quick-terminal window and one creation request; end state is shown or hidden, never duplicated. | Two or more quick windows, duplicate shell starts, or a stuck half-shown state. |
| QT-X11-05 | Linux X11 | With a full-screen app focused, press the summon shortcut. | The confirmed grab delivers the toggle and OdyTTY requests show/focus on the existing quick window. Window-manager stacking may keep the full-screen surface above it; OdyTTY does not claim focus from the request alone. | Duplicate/crash, a confirmed grab that delivers no toggle, or a false focus claim. |
| QT-X11-06 | Linux X11 | Multi-monitor: set `quick_terminal_monitor` to `active`, then `primary`, then an index. Remove the indexed monitor while hidden and summon again. | Geometry anchors to the selected live monitor. A missing index falls back to active, then primary/first, with a bounded on-screen size. | Stale off-screen placement, zero-size window, duplicate, or crash. |
| QT-X11-07 | Linux X11 | Virtual desktop: summon on desktop A, switch to desktop B, summon or hide. | The same quick-window identity receives show/focus or hide. The window manager decides desktop migration; OdyTTY never spawns a desktop-specific copy or loses the session. | Second instance, false focus claim, or lost session across desktops. |
| QT-X11-08 | Linux X11 | Startup: cold start with `quick_terminal = on`. | First ordinary local terminal is usable without waiting on quick-terminal registration. | First prompt delayed until shortcut registration finishes. |
| QT-X11-09 | Linux X11 | Display reconfig: unplug/replug or change scale while hidden, then summon. | The next show recomputes selection and bounded geometry from the current monitor snapshot. | Stale off-screen placement, crash, or zero-size window. |
| QT-X11-10 | Linux X11 | Crash recovery: kill the process with the quick terminal open, then hidden; restart each time. | The quick PTY is not crash-persistent. Only the last ordinary snapshot follows restoration rules; no quick window exists until the first summon, which creates one. | Quick state absorbed into ordinary restore, startup duplicate, or claim that the killed PTY survived. |
| QT-X11-11 | Linux X11 | Enable hide-on-focus-loss. Open an overlay or copy/hint/rename mode in the quick window, and separately open the process merge picker; cause a focus transition. | The interaction-owning quick window stays visible. After the interaction closes, the next genuine focus loss hides it without ending the session. | Overlay/picker hidden mid-interaction, stranded input ownership, or session exit. |
| QT-X11-12 | Linux X11 | Reserve the configured shortcut in another application or the window manager, then start OdyTTY. | No success is reported. The in-app notice and log contain (trigger substituted for `{trigger}`): `X11 could not register {trigger} because another application or the window manager already holds it. Release that binding or choose a different quick_terminal_shortcut, then restart OdyTTY.` | Silent failure, generic error, or a false registered claim after `BadAccess`. |

### 1.2 Linux Wayland with GlobalShortcuts portal

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| QT-WL-01 | Wayland + portal | Enable quick terminal; complete any portal permission prompt for the shortcut. On Hyprland also add `bind = , F12, global, io.unfinished_works.odytty:odytty_quick_terminal_summon` to the compositor configuration, since the portal accepts the shortcut without attaching a key. | OdyTTY first registers `io.unfinished_works.odytty` through `org.freedesktop.host.portal.Registry.Register` on its portal connection. After confirmation, the shortcut summons/hides like X11 QT-X11-01..04. | `CreateSession` rejects the host with `An app id is required`, registration reports success without a portal grant, or the grab never works. |
| QT-WL-02 | Wayland + portal | Deny or dismiss the portal permission. | No success is reported. The in-app notice and log contain (trigger substituted for `{trigger}`): `The Wayland GlobalShortcuts portal did not grant {trigger} (response code 1). Allow the shortcut in your desktop's portal prompt or choose a different quick_terminal_shortcut, then restart OdyTTY. Alternatively, bind {trigger} in your compositor to run: odytty control quick-terminal toggle, with automation_endpoint = on. Toggle Quick Terminal in the command palette also works from an ordinary window.` | Silent failure, generic error, or a false "registered" state with a dead shortcut. |
| QT-WL-03 | Wayland + portal | Repeat QT-X11-05..11 under Wayland. | Same expected/fail criteria as the X11 rows, subject to compositor policy. | Silent no-op, duplicate instances, hidden interaction, or startup delay of the default terminal. |

### 1.3 Linux Wayland without portal (actionable limitation)

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| QT-WL-NP-01 | Wayland, no `org.freedesktop.portal.GlobalShortcuts` | Enable `quick_terminal` and `automation_endpoint`, then attempt portal registration. | Product shows this exact actionable limitation (trigger substituted for `{trigger}`): `Wayland does not let applications grab a global shortcut directly, so {trigger} cannot be registered here. To use the compositor fallback, bind {trigger} in your compositor to run: odytty control quick-terminal toggle, with automation_endpoint = on. You can also use a desktop that provides the org.freedesktop.portal.GlobalShortcuts portal, run OdyTTY under X11, or use Toggle Quick Terminal from the command palette in an ordinary window.` | Silent failure, empty notice, or a false "registered" claim. |
| QT-WL-NP-02 | Wayland, no portal | Bind F12 in Sway with `bindsym F12 exec odytty control quick-terminal toggle` or in Hyprland with `bind = , F12, exec, odytty control quick-terminal toggle`; press it twice with one eligible OdyTTY running. | The same dedicated quick terminal summons, then hides; the session is preserved and no second ordinary process/window is spawned. | A second ordinary window, a dead compositor action, duplicate quick windows, or silent failure. |

### 1.4 macOS

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| QT-MAC-01 | macOS | Enable quick terminal and verify success. Then reserve the same shortcut in another application and restart. | A confirmed registration summons/hides and preserves the session. A rejected registration reports no success; the in-app notice and log contain (trigger substituted for `{trigger}`): `macOS did not grant {trigger} to OdyTTY, usually because another application already holds it. Release that shortcut or choose a different quick_terminal_shortcut, then restart OdyTTY. The OdyTTY log records the OSStatus.` | Shortcut dead after a success claim, session restart on hide, silent rejection, or generic failure text. |
| QT-MAC-02 | macOS | Full-screen Space, multi-display, Mission Control desktop switch, display reconfig, cold start, crash-while-hidden, and focus loss during an overlay/picker. | Same identity/session, fallback, startup, restore-exclusion, and interaction-ownership contracts as QT-X11-04..11. Carbon/macOS decides Space activation and stacking; a focus request is not a focus claim. | Duplicate quick windows, restoration as ordinary window, hidden interaction, false focus claim, or first-frame delay. |

### 1.5 Windows

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| QT-WIN-01 | Windows | Enable quick terminal and verify success. Then reserve the same shortcut in another application and restart. | A confirmed registration summons/hides one preserved session. `ERROR_HOTKEY_ALREADY_REGISTERED` reports no success; the in-app notice and log contain (trigger substituted for `{trigger}`): `Windows could not register {trigger} because another application already holds it (ERROR_HOTKEY_ALREADY_REGISTERED). Release that binding or choose a different quick_terminal_shortcut, then restart OdyTTY.` | Duplicate windows, lost session, generic error, silent rejection, or a success claim for the conflict. |
| QT-WIN-02 | Windows | Full-screen game/app, multi-monitor, virtual desktop, cold start, display change, crash-while-hidden, and focus loss during an overlay/picker. | Same identity/session, fallback, startup, restore-exclusion, and interaction-ownership contracts as QT-X11-04..11. Windows decides foreground activation and virtual-desktop migration; a focus request is not a focus claim. | Duplicate, hidden interaction, false focus claim, default-terminal startup delay, or display-change crash. |

---

## 2. External file drop

**Shared policy:** dropped local paths use shell-aware quoting, open the existing
risky-paste confirmation when required (`warn_on_risky_paste`), never append
Enter, and never auto-execute. Confirm with **Paste**; cancel with **Cancel**.
**Paste as One Line** must not appear for file-drop batches.

### 2.1 Linux (X11 and Wayland)

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| FD-L-01 | Linux X11 + Wayland | Idle bash prompt; drop a file whose name contains spaces and quotes (synthetic fixture). | Confirmation preview lists the path; Paste inserts a shell-safe quoted path; no trailing Enter; shell does not run the path as a command. | Enter appended, unquoted metacharacters, or immediate execution. |
| FD-L-02 | Linux | Drop a Unicode filename (non-ASCII). | Lossless insert after confirm, or refuse with `This path cannot be inserted losslessly with the active shell. Copy the path manually or cancel.` | Moji-bake, truncated path, or silent drop. |
| FD-L-03 | Linux | Drop multiple files in one gesture. | Preview order matches drop order; Paste inserts all quoted paths in that order without Enter. | Reordered, truncated list, or Enter after the batch. |
| FD-L-04 | Linux | Drop a directory. | Path inserts as a quoted directory path (not recursive contents), no Enter. | Recursive upload, open-in-file-manager side effect, or Enter. |
| FD-L-05 | Linux | Drop a non-UTF-8 path fixture if the file manager can produce one. | Refuse with the UnsupportedPath notice above, or insert via the shell's non-UTF-8-safe form without corruption. Never claim success for a corrupted string. | Corrupted bytes pasted as if valid UTF-8. |
| FD-L-06 | Linux | Bash, then zsh, then fish idle prompts: drop the same spacey path. | Each shell receives its quoting form; no Enter. | Wrong shell quoting, or fish/zsh treated as bash. |
| FD-L-07 | Linux | Start a foreground job (`sleep 30`); drop a file. | Refuse with: `File insertion could not verify the local launch shell alone in the foreground. Return to the launch shell and stop its jobs, or copy the path manually if this platform cannot verify ownership.` | Path inserted while a job owns the TTY. |
| FD-L-08 | Linux | Open Settings (or Theme Picker); drop a file. | Refuse with: `Close the current dialog before dropping files.` Pending drop cleared if a dialog opens after a drop started. | Insert behind the overlay, or stuck pending confirm. |
| FD-L-09 | Linux | Focus a remote or attached pane; drop a local file. | Refuse with: `Drop files into a local shell. Remote and attached panes do not accept local paths.` | Local path written into the remote/attached session. |
| FD-L-10 | Linux | Oversized batch (more than 128 files or more than 256 KiB of path bytes). | Refuse with: `Drop fewer files (at most 128 files and 256 KiB of paths).` | Partial insert or hang. |
| FD-L-11 | Linux | Cancel mid-confirm; drop again. | Cancel clears pending state; second drop starts clean. | Stale preview, double insert, or Paste of the cancelled batch. |

### 2.2 macOS

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| FD-MAC-01 | macOS | Repeat FD-L-01..04 and FD-L-06..11 with Finder drops into a local shell. | Same notices and no-Enter contract as Linux. | Platform-specific silent accept on remote panes or while a job runs. |
| FD-MAC-02 | macOS | Unicode and Apple-decomposed filename drop. | Lossless quoted insert or UnsupportedPath refuse. | NFD/NFC corruption accepted as success. |

### 2.3 Windows (explicit refusal)

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| FD-WIN-01 | Windows | Drop a file onto a local ConPTY shell. | Insertion is refused with: `File drop insertion is not available on Windows in this version. Copy the path manually.` | Path inserted, a success toast, or the generic job-control notice. |
| FD-WIN-02 | Windows | Drop onto a remote/attached pane or with Settings open. | Still refused; NonLocalPane / overlay notices may appear instead when those conditions apply first. | Any successful path insert. |

---

## 3. Keyboard-first window merge

User-visible command palette rows (only when another window exists):

- `Merge This Window Into...`
- `Pull Window Into This One...`

Session Navigator legend (when merge targets exist): `i merge window`,
`p pull window`.

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| WM-01 | Linux / macOS / Windows | Open two ordinary windows with distinct tabs. Palette: `Merge This Window Into...`. | Candidate windows show temporary numerals; pressing a numeral completes the merge; source window closes only after success; tabs/panes/PTYs survive in the target. | Source closes before transfer, lost PTY, or no numerals. |
| WM-02 | Linux / macOS / Windows | From the other window: `Pull Window Into This One...` and select. | Selected window's sessions move into the origin; source closes after success. | Wrong direction, duplicate tabs, or orphan window. |
| WM-03 | Linux tiling compositor without decorations | Open merge picker; confirm numerals paint inside candidate surfaces without compositor plugins. | Numerals visible and selectable from the keyboard. | Numerals missing, or merge requires a mouse-only OS picker. |
| WM-04 | All | Start merge picker; press Escape / cancel. | Picker dismisses; no transfer; both windows intact. | Partial move or stuck picker. |
| WM-05 | All | Start merge picker; close the candidate window before selecting. | Picker fails closed (no transfer, clear stale target); no panic. | Crash, merge into a dead id, or silent corruption. |
| WM-06 | All | Merge a window that has an attached session handle and a live local PTY. | Attach handle and PTY remain usable in the destination after transfer. | Detached session dropped, or PTY killed by the merge. |
| WM-07 | All | Session Navigator: with two windows, press `i` / `p` per legend. | Same merge/pull picker behavior as the palette rows. | Keys dead while legend advertises them, or merge offered with only one window. |

---

## 4. Local automation

**Prerequisites:** `automation_endpoint = on` (also `ODYTTY_AUTOMATION_ENDPOINT`).
Off by default. CLI: `odytty control --endpoint <path> ...` with
machine-readable or human-readable output and no shell interpolation of
arguments.

### 4.1 Linux

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| AU-L-01 | Linux | Start with `automation_endpoint = off`. | No control socket under `$XDG_RUNTIME_DIR/odytty/` before or after the first frame. | Socket created while the setting is off. |
| AU-L-02 | Linux | Enable `automation_endpoint = on` (config or live reload). | After the first usable frame (not before), an owner-private socket appears (name pattern `control-<pid>.sock`). Default terminal is not delayed by endpoint setup. | Socket before first frame, world-writable socket, or startup stall. |
| AU-L-03 | Linux | `odytty control --endpoint "$XDG_RUNTIME_DIR/odytty/control-<pid>.sock" list` (and `status <id>` for one object). | Machine-readable success; lists or reports non-sensitive window/workspace/tab structure only. | Terminal contents returned, secret-bearing defaults, or a bare `status` without an object id succeeding. |
| AU-L-04 | Linux | Structural actions: focus, open profile/tab/split as documented. | Actions apply; input/send-text and content-read remain unavailable. | Implicit send-text or screen scrape succeeds. |
| AU-L-04b | Linux | Open Settings (or search, copy mode, or a tab rename) in the target window, then send focus/split/rename. | Reply is the `busy` error; `status` still answers. Close the dialog and retry: the action applies. | Mutation applied beneath the open dialog, or a pane switch that leaves copy mode or rename active on the wrong pane. |
| AU-L-04c | Linux | With the quick terminal hidden, send `focus` for one of its objects. | Reply is the `unavailable` error and the quick window stays hidden; the global shortcut still summons it. | `applied` reply with no visible window. |
| AU-L-04d | Linux | Trigger an OSC 52 write confirmation (`osc52_write = ask`, then `printf '\e]52;c;aGk=\a'`) and, while it is showing, send focus/split/rename. | Reply is the `busy` error; the confirmation stays showing; `status` still answers. | Mutation applied beneath the confirmation, or the confirmation dismissed by the request. |
| AU-L-05 | Linux | Set `automation_endpoint = off` (reload). | Socket is removed; further CLI calls fail cleanly. | Stale listening socket left behind. |
| AU-L-06 | Linux | Start a second OdyTTY instance with automation on. | Second instance does not unlink the first instance's socket; each owns its endpoint. | First socket deleted when the second starts. |
| AU-L-07 | Linux | Unset or replace `XDG_RUNTIME_DIR` with a non-owner-private path. | Endpoint unavailable; notice includes: `Local automation unavailable; XDG_RUNTIME_DIR must name an owner-private runtime directory.` | Listener starts on a shared directory. |
| AU-L-08 | Linux | With one running instance and both settings on, run `odytty control quick-terminal toggle` twice. | Each call prints `accepted`; the dedicated quick terminal summons then hides without an explicit endpoint or a new process. | Discovery selects no instance, starts a new process, retries a mutation, or reports a visibility state instead of queued acceptance. |
| AU-L-09 | Linux | Exercise `QuickTerminalToggle` against the native host seam with `automation_endpoint = off`; then enable the endpoint while leaving `quick_terminal = off` and invoke the action through the CLI. | The disabled endpoint returns `permission_denied`; the disabled quick terminal returns `unavailable`. Neither request changes window visibility. | Either disabled setting accepts or applies the action, or terminal input/content authority becomes available. |
| AU-L-10 | Linux | Start two instances with both settings on and run the bare toggle; then repeat once with `--endpoint PATH` for one listed socket. | The bare form refuses and lists both eligible paths. The explicit form prints `accepted` and toggles only the selected instance. | PID-order selection, toggling both instances, omission of either path, or refusal of the explicit form. |

### 4.2 macOS

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| AU-MAC-01 | macOS | Mirror AU-L-01..06 using the owner-private state-directory endpoint path from the log/docs. | Same owner-only and no-before-first-frame contracts. | World-readable socket, or startup delay. |
| AU-MAC-02 | macOS | Force a state-directory failure (permissions). | Notice: `Local automation unavailable; check the OdyTTY log for the owner-private state-directory error.` | Silent listen on an insecure path. |

### 4.3 Windows

| # | Platforms | Action | Expected | Fail if |
| --- | --- | --- | --- | --- |
| AU-WIN-01 | Windows | Enable `automation_endpoint = on`. After the first usable frame, list pipes (for example `[System.IO.Directory]::GetFiles("\\.\pipe\")` in PowerShell). | `\\.\pipe\odytty-control-<pid>` exists only after the first frame; no TCP listener appears; `odytty control --endpoint \\.\pipe\odytty-control-<pid> capabilities` succeeds. | Pipe before first frame, any network bind, or CLI failure from the owning account. |
| AU-WIN-02 | Windows | Re-run AU-L-03..06 adapted to the pipe path; start a second OdyTTY instance; disable the setting at runtime. | Each instance owns its own pipe name; the second never takes over the first; disable removes the pipe and later CLI calls fail cleanly. | First instance endpoint stolen, or pipe still connectable after disable. |
| AU-WIN-03 | Windows, second local account | From another local user account, run the CLI against the owner's pipe. | Refused before any request is answered (access denied or owner mismatch). | Any reply from a non-owner account. |
| AU-WIN-04 | Windows, second machine | From another machine, attempt `\\<host>\pipe\odytty-control-<pid>` with the CLI and with a raw pipe client. | The CLI rejects the remote name before opening; the raw client is refused by the pipe. | Any remote connection accepted. |
| AU-WIN-06 | Windows | With the endpoint enabled, run the hostile-client suite path by hand: send a frame declaring `u32::MAX` bytes, then a valid `capabilities` request from a second client. | The first client receives a `too_large` rejection with request id 0; the second is answered normally; the notice area stays quiet. | Endpoint stops accepting after a hostile frame, or the UI reports it enabled while `capabilities` no longer answers. |
| AU-WIN-05 | Windows | If a bind or security error occurs (for example a pre-existing pipe of the same name), read the notice. | Notice: `Local automation unavailable; check the OdyTTY log for the named-pipe security or bind error.` | Silent success or listener on a foreign instance. |

---

## String verification notes

Checked against source while authoring:

| Surface | Exact user-visible string | Source |
| --- | --- | --- |
| X11 shortcut conflict | `X11 could not register {trigger} because another application or the window manager already holds it. Release that binding or choose a different quick_terminal_shortcut, then restart OdyTTY.` | `src/native/quick_terminal/mod.rs` (`x11_conflict_notice`) |
| Wayland portal refusal | `The Wayland GlobalShortcuts portal did not grant {trigger} (response code {code}). Allow the shortcut in your desktop's portal prompt or choose a different quick_terminal_shortcut, then restart OdyTTY. Alternatively, bind {trigger} in your compositor to run: odytty control quick-terminal toggle, with automation_endpoint = on. Toggle Quick Terminal in the command palette also works from an ordinary window.` | `src/native/quick_terminal/wayland.rs` (`portal_refused_reason`) |
| Wayland portal unconfirmed binding | `The Wayland GlobalShortcuts portal reported success without granting {trigger}. Choose a different quick_terminal_shortcut and restart OdyTTY, or bind {trigger} in your compositor to run: odytty control quick-terminal toggle, with automation_endpoint = on. Toggle Quick Terminal in the command palette also works from an ordinary window.` | `src/native/quick_terminal/wayland.rs` (`portal_unconfirmed_binding_reason`) |
| Wayland Registry registration failure | `The Wayland portal could not register OdyTTY's application ID with org.freedesktop.host.portal.Registry.Register before requesting {trigger}. Restart xdg-desktop-portal and OdyTTY, then try again. Alternatively, bind {trigger} in your compositor to run: odytty control quick-terminal toggle, with automation_endpoint = on.` | `src/native/quick_terminal/wayland.rs` (`portal_registry_registration_failed_reason`) |
| Wayland old portal requires app id | `The Wayland GlobalShortcuts portal requires an application ID, but this xdg-desktop-portal does not provide org.freedesktop.host.portal.Registry.Register (requires xdg-desktop-portal 1.18 or newer). Update the portal and restart OdyTTY. Until then, bind {trigger} in your compositor to run: odytty control quick-terminal toggle, with automation_endpoint = on.` | `src/native/quick_terminal/wayland.rs` (`portal_registry_required_reason`) |
| Wayland no-portal | `Wayland does not let applications grab a global shortcut directly, so {trigger} cannot be registered here. To use the compositor fallback, bind {trigger} in your compositor to run: odytty control quick-terminal toggle, with automation_endpoint = on. You can also use a desktop that provides the org.freedesktop.portal.GlobalShortcuts portal, run OdyTTY under X11, or use Toggle Quick Terminal from the command palette in an ordinary window.` | `src/native/quick_terminal/wayland.rs` (`portal_unavailable_reason`) |
| macOS shortcut refusal | `macOS did not grant {trigger} to OdyTTY, usually because another application already holds it. Release that shortcut or choose a different quick_terminal_shortcut, then restart OdyTTY. The OdyTTY log records the OSStatus.` | `src/native/quick_terminal/mod.rs` (`macos_registration_failure_notice`) |
| Windows shortcut conflict | `Windows could not register {trigger} because another application already holds it (ERROR_HOTKEY_ALREADY_REGISTERED). Release that binding or choose a different quick_terminal_shortcut, then restart OdyTTY.` | `src/native/quick_terminal/mod.rs` (`windows_conflict_notice`) |
| File drop TooLarge | `Drop fewer files (at most 128 files and 256 KiB of paths).` | `src/native/file_drop.rs` |
| File drop UnknownShell | `File insertion could not verify the local launch shell alone in the foreground. Return to the launch shell and stop its jobs, or copy the path manually if this platform cannot verify ownership.` | `src/native/file_drop.rs` |
| File drop Windows | `File drop insertion is not available on Windows in this version. Copy the path manually.` | `src/native/file_drop.rs` |
| File drop UnsupportedPath | `This path cannot be inserted losslessly with the active shell. Copy the path manually or cancel.` | `src/native/file_drop.rs` |
| File drop NonLocalPane | `Drop files into a local shell. Remote and attached panes do not accept local paths.` | `src/native/file_drop.rs` |
| Overlay open | `Close the current dialog before dropping files.` | `src/native/app/file_drop.rs` |
| Automation Linux XDG | `Local automation unavailable; XDG_RUNTIME_DIR must name an owner-private runtime directory.` | `src/native/app/automation_host.rs` |
| Automation macOS | `Local automation unavailable; check the OdyTTY log for the owner-private state-directory error.` | `src/native/app/automation_host.rs` |
| Automation Windows | `Local automation unavailable; check the OdyTTY log for the named-pipe security or bind error.` | `src/native/app/automation_host.rs` |
| Palette merge | `Merge This Window Into...` / `Pull Window Into This One...` | `src/native/palette_overlay.rs` |
| Navigator merge | `i merge window` / `p pull window` | `src/native/session_attach_overlay.rs` |
| Palette quick | `Toggle Quick Terminal` | `src/native/palette_overlay.rs` |

### Findings (docs / product gaps to watch during hands-on)

1. **Windows file drop refuses by platform.** The Windows notice names the
   platform rather than the job-control check; any successful insert is FAIL.
2. **Comment vs palette casing.** Internal comments often say
   `Merge this window into...` while the palette shows
   `Merge This Window Into...`. Checklists quote the palette.
3. **Windows automation rows need two accounts and two machines.** AU-WIN-03
   and AU-WIN-04 are the only evidence for the DACL and remote-rejection
   contracts; CI verifies construction, not behavior. SKIP with reason when the
   second account or machine is unavailable, and do not claim those contracts.
4. **Wayland without a portal (QT-WL-NP-02)** uses the compositor-invocable
   automation toggle with both opt-in settings enabled. Unix discovery refuses
   ambiguity; the palette action remains available from an ordinary window.
