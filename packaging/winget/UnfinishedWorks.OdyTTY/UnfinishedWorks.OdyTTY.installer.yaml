# PackageIdentifier is case-sensitive and effectively immutable once accepted
# into microsoft/winget-pkgs (lookup is case-insensitive for users).
PackageIdentifier: UnfinishedWorks.OdyTTY
PackageVersion: 0.7.0
InstallerType: zip
NestedInstallerType: portable
NestedInstallerFiles:
  - RelativeFilePath: odytty.exe
    PortableCommandAlias: odytty
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/ghreprimand/odytty/releases/download/v0.7.0/odytty-0.7.0-windows-x86_64.zip
    # Replace with the SHA256 for odytty-0.7.0-windows-x86_64.zip from SHA256SUMS
    # when preparing the post-release winget submission.
    InstallerSha256: REPLACE_WITH_SHA256_FROM_SHA256SUMS
ManifestType: installer
ManifestVersion: 1.9.0
